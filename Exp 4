Aim
To write a Python program to perform and demonstrate various list operations such as length, append, insert, remove, sort, reverse, copy, maximum, minimum, sum, extend, and pop.
Algorithm
Start the program.
Create a list numbers = [50, 20, 30, 10, 40, 20].
Display the original list.
Find and display the length of the list using len().
Add 60 at the end using append().
Insert 25 at index 2 using insert().
Remove the first occurrence of 20 using remove().
Arrange the list in ascending order using sort().
Reverse the order of elements using reverse().
Create a copy of the list using copy().
Find and display the maximum value using max().
Find and display the minimum value using min().
Calculate and display the sum of all elements using sum().
Add [30, 20] to the list using extend().
Remove the last element using pop() and display the popped element.
Stop the program.

numbers = [50,20,30,10,40,20]
print("Original List:",numbers)

#length
print("Length of list",len(numbers))

#append
numbers.append(60)
print("After append:",numbers)

#insert
numbers.insert(2,25)
print("After insert(2,25)",numbers)

#remove
numbers.remove(20)
print("After removing:",numbers)

#sort
numbers.sort()
print("After sort:",numbers)

#reverse
numbers.reverse()
print("After reversal:",numbers)

#copy
new_list = numbers.copy()
print("After copy",new_list)

#max
print("Max is:",max(numbers))

#min
print("Min is:",min(numbers))


#sum
print("Total sum is:",sum(numbers))


#extend
numbers.extend([30,20])
print("After extend",numbers)

#pop
top_ele = numbers.pop()
print("After pop():",numbers)
print("Popped item:",top_ele)

Output
Original List: [50, 20, 30, 10, 40, 20]
Length of list 6
After append: [50, 20, 30, 10, 40, 20, 60]
After insert(2,25) [50, 20, 25, 30, 10, 40, 20, 60]
After removing: [50, 25, 30, 10, 40, 20, 60]
After sort: [10, 20, 25, 30, 40, 50, 60]
After reversal: [60, 50, 40, 30, 25, 20, 10]
After copy [60, 50, 40, 30, 25, 20, 10]
Max is: 60
Min is: 10
Total sum is: 235
After extend [60, 50, 40, 30, 25, 20, 10, 30, 20]
After pop(): [60, 50, 40, 30, 25, 20, 10, 30]
Popped item: 20
